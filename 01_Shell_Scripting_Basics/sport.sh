#!/usr/bin/env bash

# Q :- Create a script named sport.sh, make it executable, Accept two parameters: a name and a favorite sport display any setence to the console using those inputs 

NAME=$1
FAV_SPORT=$2

echo Hello $NAME, your favorite sport is $FAV_SPORT